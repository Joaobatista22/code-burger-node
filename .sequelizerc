const { resolve } = require('path')
module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src/app/model'), //define o caminho dos models
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), //diretorio onde ficarão as migrations
}
